для коректной работы должен быть установлен node, npm, yarn , проверь:
    node -v
    npm -v
    yarn -v

в директории parse_module/drivers/selenium_with_puppeteer запусти yarn init ->
    это установит необходимые зависимости из package.json в той же директории
